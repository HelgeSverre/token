// Mixed tabs and spaces - useful for testing indentation detection

Spaces only:
    4 spaces
        8 spaces

Tabs only:
	1 tab
		2 tabs
			3 tabs

Mixed (spaces then tabs):
    	spaces then tab
        	8 spaces then tab

Mixed (tabs then spaces):
	    tab then 4 spaces
		    2 tabs then 4 spaces

Alternating:
 	 	 space-tab-space-tab-space
	 	 	tab-space-tab-space-tab

Real-world mess (inconsistent indentation):
  2 spaces
   3 spaces
    4 spaces
	tab
	 tab + space
  	2 spaces + tab

// This kind of mess is common in poorly maintained codebases
function example() {
    // 4 spaces
	// 1 tab
  // 2 spaces
   // 3 spaces
}
